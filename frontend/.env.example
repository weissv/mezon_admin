# Backend API URL (for Vite proxy or production backend connection)
VITE_API_URL=http://localhost:4000/api

# Enable Figma MCP Capture Script (optional, for visual capture in browser during development)
VITE_ENABLE_FIGMA_CAPTURE=true
