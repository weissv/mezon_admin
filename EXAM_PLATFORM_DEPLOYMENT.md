# Платформа контрольных работ - Руководство по развертыванию

## 📋 Обзор

Платформа контрольных работ позволяет:
- Учителям, завучам и директорам создавать и редактировать контрольные
- Каждый пользователь видит только свои созданные контрольные
- Генерировать публичные ссылки для учеников (без входа в ERP)
- Автоматически проверять открытые вопросы и задачи с помощью AI

## 🗄️ Модели базы данных

### Новые таблицы:
- `Exam` - контрольные работы
- `ExamQuestion` - вопросы контрольных
- `ExamSubmission` - сдачи учеников
- `ExamAnswer` - ответы на вопросы
- `ExamTargetGroup` - целевые группы/классы

### Типы вопросов:
- `MULTIPLE_CHOICE` - несколько правильных ответов
- `SINGLE_CHOICE` - один правильный ответ
- `TEXT_SHORT` - краткий текстовый ответ
- `TEXT_LONG` - развёрнутый ответ (проверяется AI)
- `PROBLEM` - задача с числовым ответом (проверяется AI)
- `TRUE_FALSE` - верно/неверно

## 🚀 Инструкции по развертыванию

### 1. Применение миграции на сервере

```bash
# Подключение к серверу
ssh root@185.217.131.26
# Пароль: Z@pu2laGYwsSF?N$

# Перейти в директорию проекта
cd /path/to/mezon_admin/backend

# Применить миграцию
npx prisma migrate deploy

# Или создать новую миграцию (в dev режиме)
npx prisma migrate dev --name add_exam_models
```

### 2. Загрузка тестовой контрольной по экономике

```bash
# В директории backend
npx ts-node prisma/seed_economics_exam.ts
```

### 3. Настройка AI проверки (опционально)

Добавьте в `.env`:
```env
OPENAI_API_KEY=sk-your-api-key
OPENAI_MODEL=gpt-4o-mini
PUBLIC_EXAM_BASE_URL=https://erp.mezon.uz/exam
```

## 🔗 API Endpoints

### Защищённые маршруты (для учителей)
- `GET /api/exams` - список контрольных (только своих)
- `POST /api/exams` - создать контрольную
- `GET /api/exams/:id` - получить контрольную
- `PUT /api/exams/:id` - обновить контрольную
- `DELETE /api/exams/:id` - удалить контрольную
- `POST /api/exams/:id/publish` - опубликовать (генерирует публичную ссылку)
- `POST /api/exams/:id/close` - закрыть приём ответов
- `GET /api/exams/:id/results` - результаты сдач
- `POST /api/exams/answers/:answerId/grade` - ручная оценка ответа

### Публичные маршруты (для учеников)
- `GET /api/public/exams/:token` - получить контрольную по токену
- `POST /api/public/exams/:token/start` - начать сдачу
- `POST /api/public/exams/:token/submit` - отправить ответы
- `GET /api/public/exams/result/:submissionId` - результат сдачи

## 🎨 Frontend страницы

- `/exams` - список контрольных
- `/exams/new` - создание новой контрольной
- `/exams/:id/edit` - редактирование контрольной
- `/exams/:id/results` - просмотр результатов
- `/exam/:token` - публичная страница для учеников (без авторизации)

## 🤖 AI Проверка

Для вопросов типа `TEXT_LONG` и `PROBLEM`:
1. При отправке ответов запускается асинхронная AI проверка
2. AI анализирует ответ на соответствие ключевым точкам
3. Результат сохраняется в `aiScore`, `aiFeedback`, `aiChecked`
4. Учитель может подтвердить или изменить оценку вручную

## 📊 Система оценивания

- Автоматическая проверка для `SINGLE_CHOICE`, `MULTIPLE_CHOICE`, `TRUE_FALSE`, `TEXT_SHORT`
- AI проверка для `TEXT_LONG`, `PROBLEM`
- Процент правильности = (набранные баллы / максимум) × 100
- Статус `PASSED` если процент >= проходной балл контрольной

## 🔐 Права доступа

Создавать/редактировать контрольные могут:
- `DIRECTOR` - директор
- `DEPUTY` - завуч
- `TEACHER` - учитель
- `DEVELOPER` - разработчик

Каждый пользователь видит и может редактировать только свои контрольные.

## ⚠️ Важные замечания

1. **Публичные ссылки** генерируются автоматически при публикации контрольной
2. **Таймер** - если установлен лимит времени, он отслеживается на фронтенде и сервере
3. **Одна попытка** - каждый ученик может сдать контрольную только один раз
4. **AI fallback** - если OpenAI API недоступен, используется проверка по ключевым точкам
